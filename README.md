# Key Overlord

A keyboard remapping program that can map multiple keys to multiple keys

- Customizable using the file `config.hpp`
- Supports hot swapping of multiple keyboards
- Fast and stable: no memory allocation at all
- No dependencies other than libc
